name: Run Ruff

on:
  push:
  pull_request:
  # schedule:
  #   - cron: "0 0 * * *"

jobs:
  validate:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v5
      - name: Run Ruff
        uses: astral-sh/ruff-action@v1
